### Library for PGP key verification using DNSSEC

See `test-rlib.c` for example usage. Build and run using `make test` inside the provided vagrant box.

### Download Vagrant Box:

```
$ bash init-box.sh
```
